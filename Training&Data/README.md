Historical training assets, notebooks, experiment outputs, and legacy helpers live here.

- `notebooks/` keeps research notebooks and exploratory work.
- `output/` keeps archived generated CSV output.
- `legacy/` keeps older helper scripts and prototypes that are not part of the main app runtime.
